# API Surface

This public repo exposes the reduced Dashgithub control-plane API.

## Main module groups
- `/api/projects`
- `/api/memory`
- `/api/remote`
- `/api/localops`
- `/api/mailcenter`
- `/api/orchestration`
- `/api/modules/system`
- `/api/health`
- `/api/modules`

Use the running review stack and `/docs` from FastAPI for route-level inspection.
